{
  "name": "notes-api",
  "version": "3.2.0",
  "private": true,
  "license": "MIT",
  "main": "dist/server.js",
  "scripts": {
    "prebuild": "turbo run build --filter=notes-api^...",
    "predev": "turbo run build --filter=notes-api^...",
    "pretest": "turbo run build --filter=notes-api^...",
    "dev": "tsx watch --env-file=../../.env src/server.ts | pino-pretty",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "app-config": "3.2.0",
    "express": "5.2.1",
    "markdown": "3.2.0",
    "mdm-logger": "1.0.0",
    "mdm-util": "3.2.0",
    "pino-http": "11.0.0",
    "remark": "13.0.0",
    "services": "2.3.3",
    "remark-gfm": "1.0.0"
  }
}

{
  "name": "flag-manager",
  "version": "1.0.9",
  "private": true,
  "main": "dist/server.js",
  "scripts": {
    "prebuild": "turbo run build --filter=flag-manager^...",
    "predev": "turbo run build --filter=flag-manager^...",
    "pretest": "turbo run build --filter=flag-manager^...",
    "dev": "tsx watch --env-file=../../.env src/server.ts | pino-pretty",
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint src --ext .ts",
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "app-config": "1.8.0",
    "express": "5.2.1",
    "mdm-logger": "1.0.0",
    "mdm-util": "1.8.0",
    "pino-http": "11.0.0",
    "redis": "5.10.0",
    "services": "1.8.0"
  }
}

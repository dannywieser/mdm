{
  "name": "image-server",
  "version": "1.2.1",
  "private": true,
  "main": "dist/server.js",
  "scripts": {
    "prebuild": "turbo run build --filter=image-server^...",
    "predev": "turbo run build --filter=image-server^...",
    "pretest": "turbo run build --filter=image-server^...",
    "dev": "tsx watch --env-file=../../.env src/server.ts | pino-pretty",
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint src --ext .ts",
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "express": "5.2.1",
    "mdm-logger": "1.0.0",
    "mdm-util": "1.10.0",
    "pino-http": "11.0.0",
    "redis": "5.10.0"
  }
}

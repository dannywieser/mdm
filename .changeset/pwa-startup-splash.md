---
"web": patch
---

Show a static notebook loading icon and matching theme background on initial page load to avoid a blank white flash before the app's JS bundle mounts (especially noticeable when launching as an installed PWA).

---
"web": minor
---

Add HomeStats card to the home page displaying vault overview stats (totals, notes created by period, trends, and a notes-per-day area chart for the past year). Display is configurable via the `homeStats.show` section in `app.config.json`.

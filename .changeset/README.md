# Changesets

Use Changesets to capture user-visible changes in PRs.

- Run `npm run changeset` from repository root to add an entry in this folder.
- Keep the summary focused on user-visible behavior.
- The repo is configured as a single fixed release track across all workspaces.
- Run `npm run changeset:version` to apply pending entries and update versions/changelogs.

---
"web": minor
"mdm-util": minor
---

Render dashboard gallery preview cards as a photo pile: covers are square-cornered prints with a white border, scattered and overlapping, tilted by a deterministic amount derived from each note's ID, and lifted above the pile on hover or focus. Adds a `hashString` helper to `mdm-util`.

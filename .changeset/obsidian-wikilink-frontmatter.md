---
"markdown": minor
---

Resolve Obsidian wikilink syntax (`[[path]]` and `"[[path|alias]]"`) in frontmatter values during parsing, so fields like `cover` produce a plain file path rather than a raw wikilink string.

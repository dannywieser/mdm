---
"web": minor
---

NotesGallery now defaults to a flex (CSS multi-column masonry) layout where each card takes its natural height; the previous grid layout is still available by setting `layout: "grid"` on the view in `app.config.json`.

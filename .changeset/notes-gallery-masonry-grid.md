---
"web": patch
---

Replace the NotesGallery flex/grid layout toggle with a single CSS grid masonry layout that uses `grid-auto-flow: dense` and per-card row spans to pack covers of varying heights without leaving gaps.

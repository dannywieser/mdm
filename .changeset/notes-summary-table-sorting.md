---
"web": minor
---

Notes summary table columns are now sortable — click a column header to sort by it, click again to reverse direction. The last sort used for a view is remembered in local storage.
